🎯 SISALMOX - QUICK START PARA RAILWAY

═══════════════════════════════════════════════════════════════

📋 RESUMO TÉCNICO

Framework:      Laravel 10.10+
PHP:            8.3.16 ZTS
Database:       PostgreSQL (Railway)
Deployment:     Railway.app via GitHub

═══════════════════════════════════════════════════════════════

🚀 COMO FAZER DEPLOY

1. Ter o código em GitHub (seu fork ou repositório)

2. Acessar railway.app
   → Novo Projeto → Deploy from GitHub
   → Selecionar repositório sisalmox
   → Railway detecta automaticamente

3. Adicionar PostgreSQL
   → + Add Service → PostgreSQL
   → Railway configura variáveis automaticamente

4. Configurar um secret (OBRIGATÓRIO):
   → Variables → APP_KEY
   → Valor: sair do `php artisan key:generate --show` (local)

5. Fazer push para GitHub
   → Railway detecta automaticamente
   → Build começa
   → Deploy em ~3-5 minutos

6. App estará em: https://seu-nome.up.railway.app

═══════════════════════════════════════════════════════════════

⚙️ ARQUIVOS DE DEPLOY JÁ CRIADOS

✅ Procfile
   └─ Define como iniciar a aplicação
   └─ Inclui commands de migração automática

✅ railway.json
   └─ Configuração específica do Railway
   └─ Build e deploy settings

✅ nixpacks.toml
   └─ Otimizações de build (Nixpacks)
   └─ PHP 8.3 com extensões corretas

✅ .env.example (atualizado)
   └─ Configurado para PostgreSQL
   └─ APP_DEBUG=false (produção)

✅ .github/workflows/deploy.yml
   └─ CI/CD automatizado (opcional)

✅ Procfile.sh
   └─ Post-deploy script

═══════════════════════════════════════════════════════════════

📚 DOCUMENTAÇÃO CRIADA

1. RAILWAY_DEPLOYMENT.md
   → Guia passo a passo completo
   → Troubleshooting detalhado
   → Comandos úteis

2. DEPLOY_CHECKLIST.md
   → Checklist visual
   → O que fazer antes/depois/durante
   → Referência rápida

3. ARQUITETURA.md
   → Diagramas do sistema
   → Fluxo de dados
   → Endpoints e rotas

4. SEGURANCA_PRODUCAO.md
   → Configurações críticas de segurança
   → Proteção contra ataques
   → Checklist pré-deploy

5. PRODUCAO_PRONTO.txt
   → Este arquivo - visão geral
   → Status de preparação
   → Próximos passos

═══════════════════════════════════════════════════════════════

🔑 SECRETS/VARIÁVEIS NECESSÁRIAS

OBRIGATÓRIAS:
  APP_KEY          ← Gerar com: php artisan key:generate --show
  DB_HOST          ← Automático do Railway (${Postgres.PGHOST})
  DB_PORT          ← Automático: 5432
  DB_DATABASE      ← Automático (${Postgres.PGDATABASE})
  DB_USERNAME      ← Automático (${Postgres.PGUSER})
  DB_PASSWORD      ← Automático (${Postgres.PGPASSWORD})

RECOMENDADAS:
  APP_ENV=production
  APP_DEBUG=false
  LOG_LEVEL=error
  CACHE_DRIVER=file
  SESSION_DRIVER=file

OPCIONAIS (se usar email):
  MAIL_MAILER=smtp
  MAIL_HOST=smtp.seu-provedor.com
  MAIL_PORT=587
  MAIL_USERNAME=seu@email.com
  MAIL_PASSWORD=sua_senha_app
  MAIL_FROM_ADDRESS=noreply@sisalmox.com

═══════════════════════════════════════════════════════════════

✅ CHECKLIST RÁPIDO

Antes de fazer git push:
  [ ] APP_KEY gerado e pronto
  [ ] .env.example atualizado
  [ ] Nenhum .env real no repositório
  [ ] composer.json verificado
  [ ] Migrations prontas
  [ ] Testes locais passando

No Railway:
  [ ] Criar novo projeto
  [ ] Conectar GitHub
  [ ] Adicionar PostgreSQL
  [ ] Configurar APP_KEY
  [ ] Deploy automático

Após o deploy:
  [ ] Verificar logs
  [ ] Testar login
  [ ] Testar listagem de estoque
  [ ] Testar transferência
  [ ] Testar PDF

═══════════════════════════════════════════════════════════════

💡 DICAS IMPORTANTES

1. Railway usa PostgreSQL por padrão
   → Seu código já é compatível (Laravel handles both)
   → Não precisa mudar código

2. Railway fornece HTTPS automaticamente
   → Use https://seu-app.up.railway.app no APP_URL
   → Certificado SSL renovado automaticamente

3. Migrações rodam automaticamente
   → Procfile tem: release: php artisan migrate --force
   → Não precisa rodar manualmente

4. Caches são limpas automaticamente
   → Procfile otimiza: config:cache, route:cache
   → App fica mais rápido

5. Variáveis do PostgreSQL
   → Railway fornece via ${Postgres.PGHOST} etc.
   → Não precisa copiá-las manualmente

═══════════════════════════════════════════════════════════════

🆘 PROBLEMAS COMUNS

❌ "No app key"
→ Gerar: php artisan key:generate --show
→ Copiar para APP_KEY em Railway Variables

❌ "PDO Exception - Cannot connect"
→ Verificar se PostgreSQL foi adicionado no Railway
→ Confirmar DB_* variáveis

❌ "404 Not Found"
→ Verificar se APP_URL está correto
→ Confirmar que Procfile está na raiz

❌ "Composer install failed"
→ Verificar composer.json
→ Confirmar que tem PHP 8.1+ requirement

❌ "Logs are huge"
→ Verificar LOG_LEVEL=debug (não usar em produção)
→ Mudar para error

═══════════════════════════════════════════════════════════════

📞 SUPORTE RÁPIDO

Documentação:
  → RAILWAY_DEPLOYMENT.md (passo a passo)
  → SEGURANCA_PRODUCAO.md (segurança)
  → ARQUITETURA.md (technical details)
  → DEPLOY_CHECKLIST.md (verificação)

Railway Docs:
  → https://railway.app/docs
  → https://railway.app/docs/databases/postgresql

Laravel Docs:
  → https://laravel.com/docs
  → https://laravel.com/docs/deployment

═══════════════════════════════════════════════════════════════

🎉 PRÓXIMO PASSO

Abra seu terminal e execute:

git init
git add .
git commit -m "Ready for Railway deployment"
git remote add origin https://github.com/seu-usuario/sisalmox.git
git branch -M main
git push -u origin main

Depois acesse railway.app e aproveite! 🚀

═══════════════════════════════════════════════════════════════
Data: 27 de Janeiro de 2026
Status: ✅ PRONTO PARA PRODUÇÃO
═══════════════════════════════════════════════════════════════
